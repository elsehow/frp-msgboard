/* global window: false */

var App = require('../../../src/browser/App');
var HelloWorld = require('../../../src/common/HelloWorld');
var _ = require('lodash')

// describe("App", function() {

	// describe("PostBox"), function() {
		// beforeEach(function() {
		//     App.main();
		// 	this.dataset = {
		// 		 text: 'ayyyy https://github.com/baconjs/bacon.js/wiki/Diagrams is tite'
		// 		, url: 'https://github.com/baconjs/bacon.js/wiki/Diagrams'
				
		// 	}
		// })

		// it('should embed the correct URL', function() {
		// 	window.document.getElementById('postInput').value = this.dataset.text
		// 	setTimeout(function() {
		// 		var expected = this.dataset.url
		// 		var actual = window.document.getElementById('postEmbed')
		// 	},300)
		// })

	// }


 //        var expected = HelloWorld.helloUnderscore();
 //        var actual = window.document.getElementById('hello').innerHTML;
 //        expect(actual).toEqual(expected);
// };